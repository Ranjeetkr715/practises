import Foundation

class LeapYear
{
  func isLeapYear(Year : Int )
  {
     if Year % 400 == 0 || (Year % 4 == 0 && Year % 100 != 0)
	 {
	 print(Leap Year)
	 }
	
  }
  func isLeapYear(Year : 1996 )
  }